# USER SETTINGS

# Set this to "True" to download all packages and stop without setting up oBIT.
$USER_JUST_DOWNLOAD_PACKAGES = "False"

# Set this to either 32 or 64 to enforce the (target) platform address width (32 or 64 bit).
# Leave it on $null to use the address width for current machine.
$USER_PLATFORM_N_BITS = $null
